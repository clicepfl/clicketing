import { ICBDActivity, ICBDActivityRegistration } from '@/types/aliases';

export default function Attendance({
  activities,
  registrations,
}: {
  activities: ICBDActivity[];
  registrations: ICBDActivityRegistration[];
}) {
  return <p></p>;
}
